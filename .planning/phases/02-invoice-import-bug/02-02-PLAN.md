---
phase: 02-invoice-import-bug
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - test/widget/sales_input_screen_test.dart
autonomous: true
requirements:
  - TST-02
must_haves:
  truths:
    - "請求書インポート→保存→再表示のサイクルで合計金額が一致することをテストで確認できる"
    - "savedSubtotal が設定された明細が正しく計算されることをテストで確認できる"
    - "savedSubtotal がない通常明細の計算が既存通り動作することをテストで確認できる"
  artifacts:
    - path: "test/widget/sales_input_screen_test.dart"
      provides: "Widget テスト（import→save→reload 金額一致確認）+ _calculate() 単体テスト"
      min_lines: 150
      contains: "savedSubtotal"
  key_links:
    - from: "Widget テスト"
      to: "SalesInputScreen"
      via: "tester.pumpWidget"
      pattern: "MaterialApp"
    - from: "単体テスト"
      to: "_calculate()"
      via: "tester.state"
      pattern: "tester.state"
---

<objective>
請求書インポートバグ修正（Plan 01）に対する Widget テストと _calculate() 単体テストを追加する

**テストの目的：**
- D-07: Widget テスト（import → save → reload の金額一致確認）と _calculate() 単体テスト
- `savedSubtotal` が正しく保存・計算に使用されることを検証
- `savedSubtotal` がない通常明細の計算が既存通り動作することを検証（退行防止）

**テストファイル：** `test/widget/sales_input_screen_test.dart`（新規作成）
**テストパターン：** `sqflite_common_ffi` のインメモリ DB + `flutter_test` WidgetTester

**出力：** 新規テストファイル `test/widget/sales_input_screen_test.dart`
</objective>

<execution_context>
@/home/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/phases/02-invoice-import-bug/02-CONTEXT.md
@.planning/phases/02-invoice-import-bug/02-01-PLAN.md
@test/unit/services/sales_repository_test.dart

<interfaces>
Extracted patterns from test/unit/services/sales_repository_test.dart (existing test pattern):

sqfliteFfiInit()
DatabaseHelper.testDatabase = database;  // injection pattern
database.insert(...)  // seed data
await repository.method()  // test via public API
DatabaseHelper.testDatabase = null;  // cleanup

Tables needed for widget test:
- sales (id, document_number, date, customer_id, subtotal, tax_amount, total, tax_rate, status, created_at, updated_at)
- sales_items (id, sales_id, product_id, product_name, quantity, unit_price, subtotal, tax_rate)
- customers (id, display_name, formal_name, is_current, valid_to, updated_at)
- customer_contacts (id, customer_id, is_active, version, created_at)
- master_hidden (master_type, master_id, is_hidden)
- invoices, invoice_items (for import source data)

From lib/models/invoice_models.dart - InvoiceItem.subtotal getter:
```dart
int get subtotal {
  int base = quantity * unitPrice;
  if (discountAmount != null && discountAmount! > 0) return base - discountAmount!;
  if (discountRate != null && discountRate! > 0) return (base * (1 - discountRate!)).round();
  return base;
}
```

From lib/models/base_document.dart - DocumentItem:
```dart
class DocumentItem {
  final String id, productId, productName;
  final int quantity, unitPrice, subtotal;
  final double taxRate;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Widget テスト — 請求書インポート→保存→再読込の金額一致確認</name>
  <files>test/widget/sales_input_screen_test.dart</files>
  <action>
    新規ファイル `test/widget/sales_input_screen_test.dart` を作成する。
    `sqflite_common_ffi` + `flutter_test` を使用した Widget テスト。

    **ファイル構成：**

    ```dart
    import 'package:flutter_test/flutter_test.dart';
    import 'package:sqflite_common_ffi/sqflite_ffi.dart';
    import 'package:flutter/material.dart';
    import 'package:h_1/screens/sales_input_screen.dart';
    import 'package:h_1/services/database_helper.dart';
    import 'package:h_1/services/sales_repository.dart';

    void main() {
      sqfliteFfiInit();

      group('SalesInputScreen - 請求書インポート金額一致', () {
        late Database database;
        late SalesRepository repository;

        setUp(() async {
          database = await databaseFactoryFfi.openDatabase(inMemoryDatabasePath);
          DatabaseHelper.testDatabase = database;
          repository = SalesRepository();

          // Create required tables (sales, sales_items, customers, customer_contacts, master_hidden, invoices, invoice_items)
          // ... minimum columns per test pattern in sales_repository_test.dart
        });

        tearDown(() async {
          DatabaseHelper.testDatabase = null;
          await database.close();
        });

        testWidgets(
          '請求書インポート→保存→再読込で金額が一致する',
          (tester) async { ... }
        );
      });
    }
    ```

    **テストケース：請求書インポート→保存→再読込で金額が一致する**

    1. **Set up**: インメモリ DB に以下を作成
       - `customers` テーブル: テスト顧客 1 件
       - `customer_contacts` テーブル: テスト顧客の連絡先 1 件
       - `master_hidden` テーブル: 空
       - `invoices` テーブル: テスト請求書 1 件（discountあり、quantity=3, unitPrice=1000, 小計=2500 ← 値引き後 500 値引き）
       - `invoice_items` テーブル: 明細1件（product_id, description, quantity=3, unitPrice=1000, discountAmount=500）
       - `sales` テーブル（保存先用）: 空または必要最小限
       - `sales_items` テーブル: 空

       ポイント: InvoiceItem.subtotal 計算（base=3*1000=3000, discountAmount=500 → subtotal=2500）となる値を使用する
       これにより discountedUnitPrice = (2500/3).round() = 833 の丸めが発生 → 従来バグでは 3*833=2499 ≠ 2500

    2. **Import**: 
       - `SalesInputScreen` を `MaterialApp` でラップして `tester.pumpWidget()`
       - `_performImport()` を呼ぶ代わりに、テスト用の Invoice データを作成し `_performImport([invoice])` 相当の処理を行う
       - または、直接 `_items` にインポート状態の `_LineItem` を設定する

       詳細な実装方針（Claude's discretion per CONTEXT の裁量範囲）:
       - ウィジェットビルド後、state を取得して直接インポート明細を設定する方法
       ```dart
       await tester.pumpWidget(MaterialApp(home: SalesInputScreen()));
       await tester.pump();
       final state = tester.state(find.byType(SalesInputScreen));
       // state を通じて _items に import 明細を設定
       ```
       - または `_performImport` と同じ呼び出し経路を通す方法

    3. **Verify display**: 
       - 画面上の合計金額を検証（`_buildPriceRow` の小計・合計表示）
       - 期待値: subtotal=2500, tax=0（taxRate=0.0 のため）, total=2500
       - 丸め誤差で 2499 になっていないことを確認

    4. **Save**:
       - 「保存」ボタンをタップ
       - 保存が完了するまで待機

    5. **Reload**:
       - `SalesInputScreen(existingSalesId: savedId)` で再作成
       - 再表示後の合計金額が 2500 のままであることを確認

    6. **Verify saved data**:
       - `repository.getSales(id)` で保存データを直接確認
       - 明細の subtotal が 2500 であることを確認
  </action>
  <verify>
    <automated>flutter test test/widget/sales_input_screen_test.dart 2>&1 | grep -E '(PASS|FAIL|All tests passed)'</automated>
  </verify>
  <done>
    1. テストファイルが `test/widget/sales_input_screen_test.dart` に存在する
    2. import → save → reload の一連の流れで金額一致を確認するテストケースがある
    3. 値引きあり明細（subtotal が quantity*unitPrice と一致しない）をテストデータとして使用している
    4. `flutter test test/widget/sales_input_screen_test.dart` が PASS する
    5. `flutter analyze --no-fatal-infos` が通過する
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: _calculate() 単体テスト — savedSubtotal の動作検証</name>
  <files>test/widget/sales_input_screen_test.dart</files>
  <action>
    Task 1 と同じテストファイルに、`_calculate()` の動作を検証するテストグループを追加する。

    **グループ：`_calculate() - savedSubtotal`**

    以下のテストケースを含める：

    1. **savedSubtotal が設定された明細はその値が使われる**
       - 画面を開き、state の `_items` に `savedSubtotal` が設定された `_LineItem` を追加
       - 期待値: `_calculate()` が返す subtotal = savedSubtotal の値
       - 検証方法: `tester.state()` で state を取得し、`_buildPriceRow` の表示内容や保存結果で確認

    2. **savedSubtotal が null の通常明細は quantity * unitPrice が使われる**
       - `savedSubtotal: null` の `_LineItem` を追加
       - 期待値: `_calculate()` が quantity * unitPrice を計算して使用

    3. **通常明細の値引き計算は従来通り動作する**
       - discountAmount ありの明細で savedSubtotal なし
       - 期待値: 既存の値引き計算ロジックがそのまま使われる

    テスト用 `_LineItem` の作成:
    ```dart
    // savedSubtotal あり
    _LineItem(
      id: 'test-1',
      product: null,
      productName: 'テスト商品',
      quantity: 3,
      unitPrice: 833,       // ← 丸められた単価
      taxRate: 0.0,
      savedSubtotal: 2500, // ← 本来の小計
    );

    // savedSubtotal なし（通常明細）
    _LineItem(
      id: 'test-2',
      product: null,
      productName: '通常商品',
      quantity: 2,
      unitPrice: 1000,
      taxRate: 0.1,
      savedSubtotal: null,
    );
    ```

    **検証方法（Claude's discretion）：**
    - Widget テスト内で `tester.state` を使って `_items` にアクセスし、明細を設定後 `tester.pump()` で再描画
    - 小計/合計の表示 Widget（`_buildPriceRow`）のテキスト内容を `find.text` で検証
    - `setState` 後の再描画を確実に待つため `await tester.pump()` を十分に呼ぶ
  </action>
  <verify>
    <automated>flutter test test/widget/sales_input_screen_test.dart 2>&1 | grep -E '(PASS|FAIL|All tests passed|All tests passed)'</automated>
  </verify>
  <done>
    1. Task 1 のテストグループと同じファイルに `_calculate()` の単体テストグループがある
    2. `savedSubtotal` 優先、`quantity * unitPrice` フォールバック、値引き計算の3ケースをカバーしている
    3. `flutter test test/widget/sales_input_screen_test.dart` が全テスト PASS
    4. `flutter analyze --no-fatal-infos` が通過する
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test DB → Widget | テスト用のインメモリ DB データが Widget 描画に使われる |
| Test assertions | テスト内の期待値と実際の画面表示値の比較 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Spoofing | テストデータの作成 | mitigate | テスト用インメモリ DB は隔離されており、本番データに影響しない。tearDown で `DatabaseHelper.testDatabase = null` + `database.close()` を実行。 |
| T-02-04 | Repudiation | テストの期待値 | accept | テストの期待値は手動検証済みの計算式に基づく。将来の仕様変更時は期待値の見直しが必要。 |
</threat_model>

<verification>
1. `test/widget/sales_input_screen_test.dart` ファイルが存在する
2. テストファイルに次のテストケースが全て含まれている:
   - 請求書インポート→保存→再読込の金額一致確認（Widget テスト）
   - savedSubtotal 優先動作の確認（単体テスト）
   - savedSubtotal null フォールバックの確認（単体テスト）
   - 値引き計算維持の確認（単体テスト）
3. `flutter test test/widget/sales_input_screen_test.dart` が全テスト PASS
4. `flutter analyze --no-fatal-infos` が通過する
5. 既存の全テスト（Phase 1 のテスト含む）が退行していない:
   `flutter test` が全テスト PASS
</verification>

<success_criteria>
- Widget テストが import → save → reload の金額一致を確認する
- savedSubtotal あり/なしの両方の動作を確認するテストケースがある
- 値引き計算が維持されていることを確認するテストケースがある
- 全テストケースが `flutter test` で PASS する
- `flutter analyze --no-fatal-infos` が通過する
- Phase 1 のテストを含む既存全テストが退行していない
</success_criteria>

<output>
After completion, create `.planning/phases/02-invoice-import-bug/02-02-SUMMARY.md`
</output>
