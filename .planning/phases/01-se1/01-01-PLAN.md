---
phase: 01-se1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/services/sales_repository.dart
  - lib/services/quotation_repository.dart
autonomous: true
requirements: [BUG-01]
user_setup: []

must_haves:
  truths:
    - "SalesRepository.getSales() が返す Sales オブジェクトに明細が含まれている"
    - "SalesRepository.getAllSales() が返す各 Sales オブジェクトに明細が含まれている"
    - "SalesRepository.getSalesByInvoiceId() が返す各 Sales オブジェクトに明細が含まれている"
    - "QuotationRepository.getAllQuotations() が返す各 Quotation オブジェクトに明細が含まれている"
    - "既存の saveSales 動作に影響を与えない"
  artifacts:
    - path: lib/services/sales_repository.dart
      provides: "3つの読み取りメソッドで _loadSalesItems が呼ばれる"
      contains: "_loadSalesItems"
    - path: lib/services/quotation_repository.dart
      provides: "getAllQuotations で quotation_items がロードされる"
      contains: "_loadQuotationItems"
  key_links:
    - from: "SalesRepository.getSales()"
      to: "SalesRepository._loadSalesItems()"
      pattern: "_loadSalesItems\\(sales\\.id\\)"
    - from: "SalesRepository.getAllSales()"
      to: "SalesRepository._loadSalesItems()"
      pattern: "_loadSalesItems\\(sales\\.id\\)"
    - from: "SalesRepository.getSalesByInvoiceId()"
      to: "SalesRepository._loadSalesItems()"
      pattern: "_loadSalesItems\\(sales\\.id\\)"
    - from: "QuotationRepository.getAllQuotations()"
      to: "QuotationRepository._loadQuotationItems()"
      pattern: "_loadQuotationItems\\(quotation\\.id\\)"
---

<objective>
SE1売上伝票バグ修正 — SalesRepository と QuotationRepository の読み取りメソッドに子テーブル（sales_items / quotation_items）の明細ロード処理を追加する。

**Purpose:** 売上伝票の保存後に再表示する際、明細が空になるバグの根本原因を修正する。
**Output:** 修正済みの `lib/services/sales_repository.dart` と `lib/services/quotation_repository.dart`
</objective>

<execution_context>
@/home/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-se1/01-CONTEXT.md

<interfaces>
`Sales` model (lib/models/sales_model.dart):
- `Sales.fromMap(Map<String, dynamic> map, Customer? customer)` — ヘッダー情報のみマッピング。items は常に空配列（D-06）
- `sales.items` は `List<DocumentItem>` — ミュータブルフィールド（setter 可）
- `Sales.id` は String 型の一意識別子

`Quotation` model (lib/models/quotation_model.dart):
- `Quotation.fromMap(Map<String, dynamic> map, Customer? customer)` — 同様に items: []
- `quotation.items` も `List<DocumentItem>`
- `Quotation.id` は String 型

`DocumentItem` (lib/models/base_document.dart):
- `DocumentItem.fromMap(Map<String, dynamic> map)` — sales_items / quotation_items 両方の行に対応
- 必要なフィールド: id, product_id, product_name, quantity, unit_price, subtotal, tax_rate, notes（nullable）

Existing pattern in `SalesRepository._loadSalesItems(String salesId)` (L173-181):
```dart
Future<List<DocumentItem>> _loadSalesItems(String salesId) async {
  final database = await _db.database;
  final maps = await database.query(
    'sales_items',
    where: 'sales_id = ?',
    whereArgs: [salesId],
  );
  return maps.map((map) => DocumentItem.fromMap(map)).toList();
}
```

Reference pattern in `purchase_order_repository._fetchItems(DatabaseExecutor db, String orderId)`:
```dart
Future<List<PurchaseOrderItem>> _fetchItems(DatabaseExecutor db, String orderId) async {
  final rows = await db.query('purchase_order_items', where: 'order_id = ?', whereArgs: [orderId]);
  return rows.map(PurchaseOrderItem.fromMap).toList();
}
```

Correct pattern in `SalesRepository.getAllSalesWithItems()` (L208-250) — uses async for-loop + `_loadSalesItems`:
```dart
for (var map in maps) {
  // customer lookup...
  final sales = Sales.fromMap(map, customer);
  sales.items = await _loadSalesItems(sales.id);  // ← このパターン
  // ...
  salesList.add(sales);
}
```
</interfaces>

**Current behavior (bug):** `getSales()`, `getAllSales()`, `getSalesByInvoiceId()` が `Sales.fromMap()` 後に `_loadSalesItems()` を呼んでいないため、items が常に空配列になる。

**Database tables:**
- `sales` テーブル: id, document_number, date, customer_id, subtotal, tax_amount, total, tax_rate, notes, subject, status, invoice_ids, invoice_id, created_at, updated_at
- `sales_items` テーブル: id, sales_id, product_id, product_name, quantity, unit_price, subtotal, tax_rate, notes
- `quotations` テーブル: id, document_number, date, customer_id, subtotal, tax_amount, total, tax_rate, notes, subject, status, created_at, updated_at
- `quotation_items` テーブル: id, quotation_id, product_id, product_name, quantity, unit_price, subtotal, tax_rate, notes
</context>

<tasks>

<task type="auto">
<name>Task 1: SalesRepository の 3 メソッドに明細ロードを追加</name>
<read_first>
- lib/services/sales_repository.dart: L17-41 (getAllSales), L44-70 (getSales), L100-125 (getSalesByInvoiceId), L173-181 (_loadSalesItems), L208-250 (getAllSalesWithItems — 正しい実装パターン)
- lib/models/sales_model.dart: L71-98 (Sales.fromMap — items: [] を確認、D-06)
</read_first>
<files>lib/services/sales_repository.dart</files>
<action>
以下の 3 メソッドを修正する。修正パターンは `getAllSalesWithItems()` (L232-233) と同一：`Sales.fromMap()` の戻り値（またはそれ相当の Sales オブジェクト）に対して `sales.items = await _loadSalesItems(sales.id)` を実行する。

**修正内容（per D-01, D-02, D-03, D-04, D-06）:**

1. **`getSales()` (L44-70):** `Sales.fromMap()` の戻り値に対して items をセットする。
   - 現状：`return Sales.fromMap(map, customer);`
   - 修正後：
     ```dart
     final sales = Sales.fromMap(map, customer);
     sales.items = await _loadSalesItems(sales.id);
     return sales;
     ```

2. **`getAllSales()` (L17-41):** 現在は `maps.map((map) { ... }).toList()` の同期的スタイル。map コールバック内で `await _loadSalesItems()` を呼べないため、`for` ループに書き換える。
   - 現状：
     ```dart
     return maps.map((map) {
       final customerId = map['customer_id'] as String?;
       final customer = customerId != null
           ? customers.firstWhere(...) : null;
       return Sales.fromMap(map, customer);
     }).toList();
     ```
   - 修正後：
     ```dart
     final List<Sales> result = [];
     for (final map in maps) {
       final customerId = map['customer_id'] as String?;
       final customer = customerId != null
           ? customers.firstWhere(
               (c) => c.id == customerId,
               orElse: () => Customer(
                 id: customerId,
                 displayName: '不明な顧客',
                 formalName: '不明な顧客',
               ),
             )
           : null;
       final sales = Sales.fromMap(map, customer);
       sales.items = await _loadSalesItems(sales.id);
       result.add(sales);
     }
     return result;
     ```

3. **`getSalesByInvoiceId()` (L100-125):** 2 と全く同じパターンで直す。
   - 現状：同期的な `maps.map(...).toList()`
   - 修正後：`getAllSales()` と同じ async for ループパターン

4. **`D-06 遵守の確認：** `Sales.fromMap()` 自体は変更しない（items: [] のまま）。
   `_loadSalesItems()` (L173-181) も変更しない。

5. **`getAllSalesWithItems()` (L208-250) との関係：** このメソッドは既に正しく明細をロードしているので変更しない。修正後も二重ロードや競合は発生しない（`getAllSales` と `getAllSalesWithItems` は別メソッドであり、両方 `_loadSalesItems` を呼ぶが同じテーブルから毎回クエリするだけなので問題ない）。
</action>
<verify>
<automated>
flutter analyze --no-fatal-infos && \
grep -c "_loadSalesItems" lib/services/sales_repository.dart | xargs test 4 -eq
</automated>
</verify>
<acceptance_criteria>
- `grep -c "_loadSalesItems" lib/services/sales_repository.dart` → 4（変更前は getAllSalesWithItems 内の1ヶ所のみ → 修正後は getSales + getAllSales + getSalesByInvoiceId + getAllSalesWithItems の4ヶ所）
- `flutter analyze --no-fatal-infos` が通過する
</acceptance_criteria>
<done>
- `SalesRepository.getSales()` が明細をロードして返す
- `SalesRepository.getAllSales()` が各売上の明細をロードして返す
- `SalesRepository.getSalesByInvoiceId()` が各売上の明細をロードして返す
- `Sales.fromMap()` は変更なし（items: [] 維持）
- `flutter analyze --no-fatal-infos` 通過
</done>
</task>

<task type="auto">
<name>Task 2: QuotationRepository.getAllQuotations に明細ロードを追加</name>
<read_first>
- lib/services/quotation_repository.dart: L16-40 (getAllQuotations), 全ファイルを確認
- lib/models/quotation_model.dart: L66-86 (Quotation.fromMap — items: [] を確認)
- lib/services/sales_repository.dart: L173-181 (_loadSalesItems — 同パターンの参考実装)
</read_first>
<files>lib/services/quotation_repository.dart</files>
<action>
`QuotationRepository` に `_loadQuotationItems` メソッドを新規追加し、`getAllQuotations()` 内で呼び出す（per D-05）。

**1. `_loadQuotationItems` メソッドの追加（新規）：**
quotation_repository.dart の末尾（deleteQuotation の後など、`_generateDocumentNumber` メソッドの前あたり）に追加：

```dart
/// 見積の明細をロード
Future<List<DocumentItem>> _loadQuotationItems(String quotationId) async {
  final database = await _db.database;
  final maps = await database.query(
    'quotation_items',
    where: 'quotation_id = ?',
    whereArgs: [quotationId],
  );
  return maps.map((map) => DocumentItem.fromMap(map)).toList();
}
```

**2. `getAllQuotations()` (L16-40) の修正：**
現状の同期的 map を async for ループに変更し、各 Quotation に明細をロードする。

```dart
Future<List<Quotation>> getAllQuotations() async {
  final database = await _db.database;
  final customers = await _customerRepo.getAllCustomers();

  final maps = await database.query(
    'quotations',
    orderBy: 'date DESC',
  );

  final List<Quotation> result = [];
  for (final map in maps) {
    final customerId = map['customer_id'] as String?;
    final customer = customerId != null
        ? customers.firstWhere(
            (c) => c.id == customerId,
            orElse: () => Customer(
              id: customerId,
              displayName: '不明な顧客',
              formalName: '不明な顧客',
            ),
          )
        : null;
    final quotation = Quotation.fromMap(map, customer);
    quotation.items = await _loadQuotationItems(quotation.id);
    result.add(quotation);
  }
  return result;
}
```

**補足:**
- `DocumentItem.fromMap` は `quotation_items` のカラム名（product_id, product_name, quantity, unit_price, subtotal, tax_rate, notes）に一致しているため、そのまま使用可能（確認済み）
- `Quotation.fromMap()` 自体は変更しない（items: [] 維持、D-06 準拠）
</action>
<verify>
<automated>
flutter analyze --no-fatal-infos
</automated>
</verify>
<acceptance_criteria>
- `grep -c "_loadQuotationItems" lib/services/quotation_repository.dart` → 1（新規メソッド）
- `grep -c "_loadQuotationItems" lib/services/quotation_repository.dart` と `grep -c "_loadSalesItems" lib/services/sales_repository.dart` の両方で該当メソッドが存在する
- `flutter analyze --no-fatal-infos` が通過する
</acceptance_criteria>
<done>
- `QuotationRepository.getAllQuotations()` が明細をロードして返す
- `_loadQuotationItems()` メソッドが追加されている
- `Quotation.fromMap()` は変更なし
- `flutter analyze --no-fatal-infos` 通過
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local SQLite ↔ Repository | 信頼済みのデータベースからの読み取り |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T（改ざん） | sales_items / quotation_items テーブル | accept | 子テーブルデータの整合性チェックは既存のハッシュチェーンの範囲外。オフラインローカルアプリのため、改ざんリスクは低く許容 |
| T-01-02 | I（情報開示） | 明細データ | accept | 全データはローカル SQLite 内のみ。認証バイパスなし。追加のリスクなし |
</threat_model>

<verification>
1. `flutter analyze --no-fatal-infos` 通過
2. 修正後のコードで `_loadSalesItems` が 4 ヶ所で呼ばれていることを確認
3. `_loadQuotationItems` が 1 ヶ所で定義されていることを確認
4. 変更差分を確認（想定：sales_repository.dart 約 +20 行、quotation_repository.dart 約 +25 行）
</verification>

<success_criteria>
- [ ] `flutter analyze --no-fatal-infos` 通過
- [ ] grep で `_loadSalesItems` が 4 ヶ所呼ばれている（変更前 1 → 変更後 4）
- [ ] `_loadQuotationItems` メソッドが新規追加されている
- [ ] `Sales.fromMap()` は変更されていない（items: [] 維持）
- [ ] `Quotation.fromMap()` は変更されていない（items: [] 維持）
</success_criteria>

<output>
After completion, create `.planning/phases/01-se1/01-01-SUMMARY.md`
</output>
