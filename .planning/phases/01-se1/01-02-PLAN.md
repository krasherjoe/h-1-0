---
phase: 01-se1
plan: 02
type: execute
wave: 2
depends_on: [01-01-PLAN.md]
files_modified:
  - test/unit/services/sales_repository_test.dart
  - test/unit/services/quotation_repository_test.dart
autonomous: true
requirements: [TST-01]
user_setup: []

must_haves:
  truths:
    - "SalesRepository の明細ロード修正に対する単体テストが存在する"
    - "QuotationRepository の明細ロード修正に対する単体テストが存在する"
    - "既存のテストは壊れていない"
    - "`flutter test` が通過する"
  artifacts:
    - path: test/unit/services/sales_repository_test.dart
      provides: "SalesRepository の getSales / getAllSales / getSalesByInvoiceId の明細ロードテスト"
      min_lines: 80
      contains: "_loadSalesItems"
    - path: test/unit/services/quotation_repository_test.dart
      provides: "QuotationRepository の getAllQuotations 明細ロードテスト（既存テストに追加）"
      contains: "_loadQuotationItems"
  key_links:
    - from: "test/unit/services/sales_repository_test.dart"
      to: "lib/services/sales_repository.dart"
      via: "import + DatabaseHelper.testDatabase で in-memory DB 接続"
    - from: "test/unit/services/quotation_repository_test.dart"
      to: "lib/services/quotation_repository.dart"
      via: "import + DatabaseHelper.testDatabase で in-memory DB 接続"
---

<objective>
バグ修正箇所に対する Repository 単体テストを追加する。

**Purpose:** 修正箇所（SalesRepository の 3 メソッド + QuotationRepository.getAllQuotations）の明細ロードが正しく動作することを自動検証する。
**Output:** 新規 `test/unit/services/sales_repository_test.dart`、更新 `test/unit/services/quotation_repository_test.dart`
</objective>

<execution_context>
@/home/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-se1/01-CONTEXT.md
@test/unit/services/quotation_repository_test.dart
@test/unit/models/sales_model_test.dart
@test/widget_test.dart

<interfaces>
Testing utilities:
- `DatabaseHelper.testDatabase` — static field でモックDBを注入可能（database_helper.dart L577）
- `sqflite_common_ffi` — デスクトップ環境で SQLite をインメモリ動作させる（dev_dependency 済）
  - `sqfliteFfiInit()` — 初期化
  - `databaseFactoryFfi.openDatabase(inMemoryDatabasePath)` — インメモリDB作成

Test table schemas (sales):
```sql
CREATE TABLE sales (
  id TEXT PRIMARY KEY, document_number TEXT NOT NULL, date TEXT NOT NULL,
  customer_id TEXT, subtotal INTEGER NOT NULL, tax_amount INTEGER NOT NULL,
  total INTEGER NOT NULL, tax_rate REAL NOT NULL, notes TEXT, subject TEXT,
  status TEXT NOT NULL, invoice_ids TEXT, invoice_id TEXT,
  created_at TEXT NOT NULL, updated_at TEXT NOT NULL
);
CREATE TABLE sales_items (
  id TEXT PRIMARY KEY, sales_id TEXT NOT NULL, product_id TEXT NOT NULL,
  product_name TEXT NOT NULL, quantity INTEGER NOT NULL,
  unit_price INTEGER NOT NULL, subtotal INTEGER NOT NULL,
  tax_rate REAL NOT NULL, notes TEXT
);
CREATE TABLE customers (
  id TEXT PRIMARY KEY, display_name TEXT NOT NULL, formal_name TEXT NOT NULL,
  is_hidden INTEGER DEFAULT 0, updated_at TEXT NOT NULL
);
```

Test table schemas (quotations):
```sql
CREATE TABLE quotations (
  id TEXT PRIMARY KEY, document_number TEXT NOT NULL, date TEXT NOT NULL,
  customer_id TEXT, subtotal INTEGER NOT NULL, tax_amount INTEGER NOT NULL,
  total INTEGER NOT NULL, tax_rate REAL NOT NULL, notes TEXT, subject TEXT,
  status TEXT NOT NULL, created_at TEXT NOT NULL, updated_at TEXT NOT NULL
);
CREATE TABLE quotation_items (
  id TEXT PRIMARY KEY, quotation_id TEXT NOT NULL, product_id TEXT NOT NULL,
  product_name TEXT NOT NULL, quantity INTEGER NOT NULL,
  unit_price INTEGER NOT NULL, subtotal INTEGER NOT NULL,
  tax_rate REAL NOT NULL, notes TEXT
);
```

**Note:** `DocumentItem.fromMap()` in `lib/models/base_document.dart` handles both `sales_items` and `quotation_items` table columns (both have same column names: product_id, product_name, quantity, unit_price, subtotal, tax_rate, notes). The `loadSalesItems` / `loadQuotationItems` methods only query `sales_id`/`quotation_id` filter plus these common columns.
</interfaces>
</context>

<tasks>

<task type="auto">
<name>Task 1: SalesRepository 単体テストファイルを作成</name>
<read_first>
- test/widget_test.dart: L1-33 (sqflite_common_ffi + DatabaseHelper.testDatabase の初期化パターン)
- test/unit/models/sales_model_test.dart: L1-164 (既存 Sales モデルテストの構造)
- test/unit/services/inventory_location_repository_test.dart: L1-95 (既存リポジトリテストの構造 — setUp / tearDown / test パターン)
- lib/models/base_document.dart: L73-139 (DocumentItem.fromMap)
- lib/services/database_helper.dart: L577 (static testDatabase field)
</read_first>
<files>test/unit/services/sales_repository_test.dart</files>
<action>
`test/unit/services/sales_repository_test.dart` を新規作成する。以下のテストを含める：

**セットアップ（setUp）：**
1. `sqfliteFfiInit()` を main の先頭で1回実行
2. `databaseFactoryFfi.openDatabase(inMemoryDatabasePath)` でインメモリDB作成
3. `DatabaseHelper.testDatabase = database` で注入
4. sales, sales_items, customers テーブルを作成
5. テスト用顧客データを customers に INSERT
6. `SalesRepository` インスタンス作成

**クリーンアップ（tearDown）：**
1. `DatabaseHelper.testDatabase = null` にリセット
2. インメモリDBを `close()`

**テストケース：**

1. `getSales returns Sales with items loaded when items exist`
   - sales テーブルに1行 INSERT
   - sales_items に2行 INSERT（同一 sales_id）
   - `repository.getSales('sales-id')` を呼ぶ
   - 結果が null でない
   - `result.items.length == 2`
   - 各 item のプロパティ（productName, quantity, subtotal）が正しい

2. `getSales returns Sales with empty items when no items exist`
   - sales テーブルに1行 INSERT（sales_items には何も入れない）
   - `repository.getSales('sales-id')` を呼ぶ
   - 結果.items が空リスト

3. `getAllSales returns all Sales with items loaded`
   - sales テーブルに2行 INSERT、それぞれに異なる items を追加
   - `repository.getAllSales()` を呼ぶ
   - 結果が2件
   - 各 Sales の items が正しくロードされている

4. `getAllSales returns empty list when no sales exist`
   - 何も INSERT しない
   - `repository.getAllSales()` を呼ぶ
   - 結果が空リスト

5. `getSalesByInvoiceId returns Sales with items loaded`
   - sales テーブルに invoice_id 付きで1行 INSERT、items も追加
   - `repository.getSalesByInvoiceId('test-invoice-id')` を呼ぶ
   - 結果が1件、items がロードされている

6. `getSalesByInvoiceId returns empty list when invoice has no sales`
   - 存在しない invoice_id で検索
   - 結果が空リスト

**既存テストの影響：** `test/unit/models/sales_model_test.dart` はモデルのユニットテスト（DB不要）で、本テストファイルとは独立。競合しない。
</action>
<verify>
<automated>
cd /home/user/code/h-1.flutter.0 && flutter test test/unit/services/sales_repository_test.dart
</automated>
</verify>
<acceptance_criteria>
- `test/unit/services/sales_repository_test.dart` が存在する
- ファイルが 80 行以上
- `flutter test test/unit/services/sales_repository_test.dart` が全てのテストケースに PASS
- `flutter analyze --no-fatal-infos` が通過する
</acceptance_criteria>
<done>
- SalesRepository の全 3 読み取りメソッド（getSales, getAllSales, getSalesByInvoiceId）の明細ロードがテストされている
- 空データケースもテストされている
- `flutter test` 通過
</done>
</task>

<task type="auto">
<name>Task 2: QuotationRepository テストに明細ロードテストを追加</name>
<read_first>
- test/unit/services/quotation_repository_test.dart: L1-83（既存テスト構造 — モデル作成のみのテスト）
- lib/services/quotation_repository.dart: L16-40（getAllQuotations — 修正後の実装）
- lib/models/base_document.dart: L73-139（DocumentItem.fromMap）
</read_first>
<files>test/unit/services/quotation_repository_test.dart</files>
<action>
既存の `test/unit/services/quotation_repository_test.dart` に新しいテストグループ `QuotationRepository - items loading` を追加する。

既存の `main()` 関数内に追加：

```dart
group('QuotationRepository - items loading', () {
  late Database database;
  late QuotationRepository repository;

  setUp(() async {
    database = await databaseFactoryFfi.openDatabase(inMemoryDatabasePath);
    DatabaseHelper.testDatabase = database;
    repository = QuotationRepository();

    await database.execute('''
      CREATE TABLE quotations (
        id TEXT PRIMARY KEY, document_number TEXT NOT NULL, date TEXT NOT NULL,
        customer_id TEXT, subtotal INTEGER NOT NULL, tax_amount INTEGER NOT NULL,
        total INTEGER NOT NULL, tax_rate REAL NOT NULL, notes TEXT, subject TEXT,
        status TEXT NOT NULL, created_at TEXT NOT NULL, updated_at TEXT NOT NULL
      )
    ''');

    await database.execute('''
      CREATE TABLE quotation_items (
        id TEXT PRIMARY KEY, quotation_id TEXT NOT NULL, product_id TEXT NOT NULL,
        product_name TEXT NOT NULL, quantity INTEGER NOT NULL,
        unit_price INTEGER NOT NULL, subtotal INTEGER NOT NULL,
        tax_rate REAL NOT NULL, notes TEXT
      )
    ''');
  });

  tearDown(() async {
    DatabaseHelper.testDatabase = null;
    await database.close();
  });

  test('getAllQuotations returns quotations with items loaded', () async {
    // INSERT a quotation
    await database.insert('quotations', {
      'id': 'quote-1',
      'document_number': 'Q-001',
      'date': DateTime.now().toIso8601String(),
      'subtotal': 50000,
      'tax_amount': 5000,
      'total': 55000,
      'tax_rate': 0.1,
      'status': 'draft',
      'created_at': DateTime.now().toIso8601String(),
      'updated_at': DateTime.now().toIso8601String(),
    });
    // INSERT items
    await database.insert('quotation_items', {
      'id': 'qi-1',
      'quotation_id': 'quote-1',
      'product_id': 'prod-1',
      'product_name': '商品A',
      'quantity': 1,
      'unit_price': 30000,
      'subtotal': 30000,
      'tax_rate': 0.1,
    });
    await database.insert('quotation_items', {
      'id': 'qi-2',
      'quotation_id': 'quote-1',
      'product_id': 'prod-2',
      'product_name': '商品B',
      'quantity': 2,
      'unit_price': 10000,
      'subtotal': 20000,
      'tax_rate': 0.1,
    });

    final results = await repository.getAllQuotations();

    expect(results.length, 1);
    expect(results.first.id, 'quote-1');
    expect(results.first.items.length, 2);
    expect(results.first.items[0].productName, '商品A');
    expect(results.first.items[0].quantity, 1);
    expect(results.first.items[1].productName, '商品B');
    expect(results.first.items[1].quantity, 2);
  });

  test('getAllQuotations returns empty items when no items exist', () async {
    await database.insert('quotations', {
      'id': 'quote-empty',
      'document_number': 'Q-002',
      'date': DateTime.now().toIso8601String(),
      'subtotal': 0,
      'tax_amount': 0,
      'total': 0,
      'tax_rate': 0.1,
      'status': 'draft',
      'created_at': DateTime.now().toIso8601String(),
      'updated_at': DateTime.now().toIso8601String(),
    });

    final results = await repository.getAllQuotations();

    expect(results.length, 1);
    expect(results.first.items, isEmpty);
  });

  test('getAllQuotations returns empty list when no quotations exist', () async {
    final results = await repository.getAllQuotations();
    expect(results, isEmpty);
  });
});
```

**既存テストの注意：** 既存のグループ `QuotationRepository` は `setUp` に `repository = QuotationRepository()` のみで DB 接続なし。これらはモデル作成のテストで DB を必要としないため、sqflite_common_ffi の初期化なしで動作する。新しいグループは独自の `setUp`/`tearDown` で DB を管理するので、既存テストに影響しない。

**必要なインポートをファイル先頭に追加：**
```dart
import 'package:sqflite_common_ffi/sqflite_ffi.dart';
import 'package:sqflite/sqflite.dart';
```
（`sqflite_common_ffi` の `databaseFactoryFfi` と `inMemoryDatabasePath`、sqflite の `ConflictAlgorithm` が必要）

もし既存ファイルに既に `sqflite` の import がある場合は重複追加しない。
</action>
<verify>
<automated>
cd /home/user/code/h-1.flutter.0 && flutter test test/unit/services/quotation_repository_test.dart
</automated>
</verify>
<acceptance_criteria>
- 既存の QuotationRepository テストが全て PASS する
- 新しい items loading グループの全テストが PASS する
- `flutter test test/unit/services/quotation_repository_test.dart` が成功する
- `flutter analyze --no-fatal-infos` が通過する
</acceptance_criteria>
<done>
- `QuotationRepository.getAllQuotations()` の明細ロードがテストされている
- 空明細ケース、空リストケースもテストされている
- 既存テストは全て PASS し続ける
- `flutter test` + `flutter analyze` 通過
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| テストコード ↔ 本番コード | テストコードは本番コードに依存。テストフレームワークは信頼済み |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | E（DoS） | テスト内 DB 作成 | accept | テスト用インメモリ DB。クリーンアップで常に close + testDatabase リセット |
</threat_model>

<verification>
1. `flutter test` が全テスト PASS することを確認
2. `flutter analyze --no-fatal-infos` 通過
3. 新規ファイル `test/unit/services/sales_repository_test.dart` の存在確認
4. `test/unit/services/quotation_repository_test.dart` に items loading グループが追加されていることを確認
</verification>

<success_criteria>
- [ ] `flutter test test/unit/services/sales_repository_test.dart` が PASS
- [ ] `flutter test test/unit/services/quotation_repository_test.dart` が PASS
- [ ] `flutter test` 全体が PASS（既存テストを壊していない）
- [ ] `flutter analyze --no-fatal-infos` 通過
- [ ] 6つのテストケース（Sales: 4 + Quotation: 3 = 7）が全て通る
</success_criteria>

<output>
After completion, create `.planning/phases/01-se1/01-02-SUMMARY.md`
</output>
